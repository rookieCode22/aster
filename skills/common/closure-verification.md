# 通用闭环验证口径（共享参考）

> 本文件是多个**可运行目标 / 动态验证场景**（尤其 pentest、graybox 黑盒阶段）共用的「闭环验证 / 判定分级」通用口径，供技能正文指向后用 read_file 读取。
> 它**不是一个独立技能**（非 SKILL.md，不会进入技能表）。各漏洞的「实际效果验证方向」「漏洞特有的闭环要点」仍由各技能正文自行给出。

## 闭环验证要求（必须遵守）

> **为什么这里是「必须」**：闭环验证属于**交付契约**而非检查建议——下列项关系到产物可被下游机器消费、coverage-ledger 完整性、审计可追溯，缺失会让整条链路失效，因此是刚性要求。这与各 skill「## 基线检查项」的「可裁剪」语义不冲突：基线是「检查论」（按上下文增删），闭环是「交付论」（不可裁剪）。

- 所有结论必须形成「前置条件/输入 → 系统处理 → 实际效果/危害 → 可复核证据」的完整证据链，重点证明风险真实生效，而不是只看响应。
- 仅凭上传成功、请求成功、状态码变化、单次报错、配置落盘、前端提示、登录跳转、返回文案等中间信号，不得直接判定 `confirmed`。
- 若只有中间信号、缺少真实效果/危害验证，结论最多为 `suspected`。
- 对写操作、上传、配置变更、认证命中、权限变更、解析触发等场景，必须补做访问、回读、触发、解析、执行或副作用验证，证明风险真正落地并说明具体危害。
- 无法闭环时，必须明确缺失环节与下一步验证建议，不得跳过验证直接下结论。

## 破坏性 / 不可逆动作的闭环边界（必须遵守）

> **为什么这里是「必须」**：本节是上节「闭环」交付契约对**不可逆动作**的强制收口——"证明危害落地"如果需要真删真改业务数据，闭环就让位于数据保护。这是不可商量的安全边界（毁数据后果不可逆），不在「基线检查项」的可裁剪语义范围内。

- **何为不可逆/破坏性动作**：对真实业务数据或状态产生你**无法自行完整还原**后果的动作——删除、`drop`/`truncate`、覆盖写、不可回退的状态流转、批量写/批量改、不可撤销的对外发送等。（可在测试内自行改回的写，如改一个字段再改回，不属此列，仍走常规"写后读"。）

- **核心规则**：这类动作的"危害落地"证明，**绝不能通过对真实业务数据执行该动作取得**。按优先级三选一：
  1. **哨兵自证（首选 → `confirmed`）**：自建一次性哨兵数据（自己创建的记录/资源）承接破坏动作 → 回读证明能力真实生效 → 尽量清理；全程不碰真实业务数据。
  2. **非破坏差分（次选 → `confirmed`）**：在不毁真实数据的前提下证明"无鉴权 + 破坏动作可达且会真实落库"——如对不存在/已失效/自建标识触发与真实操作**同构的成功语义**、写前校验绕过到"即将持久化"为止、错误/时序差分等。**此处 `confirmed` 只落在"能力/可达性"维度**（该破坏动作无鉴权可达且会真实执行）；真实数据被销毁的危害严重度按**推断单独标注**，不并入 confirmed 的危害断言、不夸大成"数据已被销毁"。
  3. **停手降级（兜底 → `suspected`）**：既建不了哨兵、又无法非破坏闭环 → 判 `suspected`，并明确写"为保护真实数据未执行破坏动作"。**禁止**对真实业务数据执行破坏动作来强行闭环。
     - **防逃生舱**：降级前必须**实际尝试并排除**哨兵自证与非破坏差分两条路径，并留痕"各试了什么、为何不可行"。在未尝试这两条的情况下以"不可逆"为由直接停 `suspected`，是逃生舱——按下文第 1 档属**未闭环**，应推回去先建哨兵/做差分。"不可逆"豁免的只是"对真实数据真执行"这一条，不豁免哨兵与差分。

- **未执行破坏动作 ≠ 缺验证（关键，防回路反噬）**：对不可逆动作，"未对真实数据执行破坏动作"按下文 `## 验证路径与未闭环判定` 的**第 3 档（路径不可用/不存在）**处理——是**合法 `needs_review`**，不算"该验没验"的 `uncovered`，不该被推回要求执行真删/真改作为补全。

- **一句话收口**：不可逆动作上，**宁可 `suspected`，不可毁真实数据**；`confirmed` 只接受哨兵自证或非破坏差分。

## 取证完整性（必须遵守）

> **为什么这里是「必须」**：取证完整性是审计可追溯的最后防线——编造证据比漏报危害更严重，会污染下游所有依赖该结论的复核、修复、合规动作。这是审计公信力的硬底线，不属于「检查论」可裁剪范畴。

证据链里引用的东西必须是你**真实看到过**的，不得虚构或想当然：

- 引用的每段源码 / 配置 / 类名 / 字段名 / 黑名单内容，必须出自你实际打开读过的文件，并标注 `file:line`；**禁止凭印象或推测编造**一段"看起来该是这样"的代码、类名或字段。
- 反编译产物（如 `.class` 反编译、闭源 jar）、第三方依赖、运行时动态生成的内容，引用时必须**标注其来源与不确定性**（如"反编译自 xxx.class，方法名可能经混淆"），不得伪装成项目源码。
- 引用的请求 / 响应 / 命令输出必须是真实发出并捕获到的，能在流量/日志中回溯到对应记录；不得贴一段"理想化"的、实际从未发送或从未收到的请求/响应当作证据。
- 凡是无法定位到真实出处的引用，要么补到能定位、要么删去，**绝不允许用编造的证据支撑任何结论**——编造证据比漏报危害更严重。

## 判定分级语义（通用）

以下 `confirmed` 含义是**动态 / 可运行场景的默认基线**。若某个 pure static skill 明确声明「当前任务无可运行目标」或「动态验证不可行动」，它可以**只覆写 `confirmed` 的证据标准**（例如：`source→sink` 可达 + 静态 PoC），但 `suspected` / `not vulnerable` 的语义，以及下文 `uncovered` / `needs_review` 的闭环裁决，仍应沿用本文件。

下表中 `confirmed / suspected / not vulnerable` 是**判定语义**（技能正文、思考、回填白盒候选时用的词）。**注意它不是 jsonl `status` 字段的取值**：报告模板里 `status` 的合法取值集合由所选模板的"发现记录"节定义（见 result-with-file），模板里**通常没有 `suspected` 这个词**。把判定语义词原样写进 `status` 会让该发现匹配不到任何归档桶而被**静默丢条**，违反「欠覆盖优于误删」。落库时按"归档桶"映射，不要照抄判定词：

| 判定语义 | 含义 | 落库归档桶（`status` 取模板里该桶对应名） |
|------|------|------|
| `confirmed` | 已用完整证据链证明真实危害/能力确实生效（读到真实数据、写操作回读生效、命令执行结果可复核等）。 | 进正文·**已确认**档（当前模板名 `confirmed`） |
| `suspected` | 只有中间信号（状态码、报错、前端提示、响应差异）或仅静态候选，尚未证明真实危害。 | 进正文·**待复核**档（当前模板名 `needs_review`）——**绝不丢条** |
| `not vulnerable` | 防护生效、无敏感数据泄露或越权效果（如稳定 401/403、无泄露的 404）。 | **覆盖证明**档（`not_vulnerable`）或**排除项**档（`false_positive`） |

> 口径：判定上**宁可标 `suspected` 也不夸大 `confirmed`**；落库时 `suspected` 归入"待复核"档留在正文，**降级 ≠ 删除**。具体 `status` 字面值以所选模板为准，不确定就读模板的"发现记录与派生"节。

## 验证路径与未闭环判定（必须遵守）

`suspected` / "待复核" **不是默认终态，也不是逃生舱**。一条发现能否停在这里，取决于它**是否还有可做却没做的验证路径**。具体某类漏洞的验证路径是什么、闭环要点有哪些，由各技能正文自行给出（本文件只给通用判据）。按下面三档统一裁决：

1. **存在适用验证路径、且尚未尝试** → 该发现**未闭环**。必须把它标记为未闭环：**优先**在 `coverage_checklist` 落 `uncovered`；若当前载体没有 checklist，则走等价的未闭环承载路径（如 `open_questions` / `open_items` / result 文件中的未闭环项）。**不得**直接落 `needs_review` 当作交付完成。
2. **验证路径已尝试但受限无法继续**（如环境/权限/防护拦截、或验证已做到能力边界为止）→ 才是**合法 `needs_review`**，须注明：已做了什么验证、缺的是哪一环、下一步建议。
3. **无任何适用验证路径** → 停在 `needs_review` **合法**，不强制升 uncovered（防误杀：路径不可用就不强求）。**对不可逆动作，"对真实业务数据执行破坏动作"本身就是不可用路径**（见上文 `## 破坏性 / 不可逆动作的闭环边界`）——既建不了哨兵又无法非破坏闭环时归入本档，不计入"该验没验"。

> 一句话：**路径可用却没走 = uncovered / 未闭环（推回去验证）；路径走过但走不动 = 合法 needs_review；路径根本不存在 = 合法 needs_review**。判定上仍宁可 `suspected` 不夸大 `confirmed`，但"宁可 suspected"只为第 2/3 档背书，不能拿它给第 1 档"该验没验"开脱。

## 通用误报控制

- 不以单一状态码定论，必须看响应体真实业务字段或可观测副作用。
- 写操作以持久化回读结果为准，不以接口即时回包单独定论。
- 对可能命中缓存/降级模板的响应，追加一次带随机参数的复验。
