你正在压缩一段长对话历史，使另一个 agent 能读后无缝继续工作。

原始 agent 指令：
{{if .INSTRUCTION}}{{.INSTRUCTION}}{{else}}(无){{end}}

既有压缩摘要（向前合并，不保留过期或重复细节）：
{{if .PREV_SUMMARY}}{{.PREV_SUMMARY}}{{else}}(无){{end}}

输出一份延续性摘要，覆盖：做了什么、进行中什么、适用哪些约束、哪些文件或符号重要。按以下模板组织：
---
## 目标

[用户要达成什么]

## 指令与约束

- [仍然生效的重要指令或约束]
- [必须继续遵守的架构/流程约束]

## 发现

[值得保留的事实、调试发现、设计决策、注意事项]

## 进展

[已完成什么、进行中什么、还剩什么]

## 相关文件 / 目录

[重要的文件、目录、函数、结构体、命令、ID 的结构化列表]
---

要求：
- 把既有压缩摘要与新对话片段合并为一份连贯的延续摘要。
- 具体技术细节原样保留：文件/目录路径、函数/结构体/类型名、命令、ID、版本、阈值、错误信息、API 字段名。
- 对话中出现 task_context.md（贯穿事实板）路径或其已确认具体值（地址/端口/凭据/端点/分支版本等）时，保留文件路径指针并优先保留这些已确认值，不得在压缩中丢弃。
- 旧工具结果已被清理的，只提及该工具运行过、详细输出已不可得。
- 删除重复、过期进度与低信号内容。
- 只输出摘要正文，不带代码围栏、不带 JSON。
