你正在为「父 Agent → 子 Agent」的交接生成一段给子 Agent 看的上下文（extra）。

handoff_to（子 Agent 工具名）：
{{.HANDOFF_TO}}

父 Agent 原始指令（仅供理解背景）：
{{.AGENT_INSTRUCTION}}

当前交接上下文（含已完成步骤摘要及文件路径指针）：
{{.PREV_SUMMARY}}

任务：输出一段面向子 Agent 的交接说明，让其清楚需要完成什么。

输出要求：
- 只输出纯文本；不要 JSON、不要 Markdown 代码块。
- 只写子 Agent 的目标/验收标准，必要时补关键约束与必须知道的上下文；不写步骤/行动清单。
- 具体技术细节必须原样保留：文件/目录路径、参数/变量/函数/类名、URL/端口/IP、数值/ID/版本号、请求响应关键字段、错误信息/状态码。精简只删冗余叙述，不删技术细节。
- 工作区路径指针（parent_workspace_root、parent_step_contexts_path、parent_plan_current_path、parent_task_context_path 等）原样保留。
- 字数建议 <= 800 字（关键信息多时可超出）。
