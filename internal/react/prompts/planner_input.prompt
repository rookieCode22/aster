{{- if .HandoffContext}}
<HANDOFF_CONTEXT>
{{.HandoffContext}}
</HANDOFF_CONTEXT>

{{end -}}
{{- if .InputTimeline}}
<INPUT_TIMELINE>
用户输入时间线：
{{.InputTimeline}}
</INPUT_TIMELINE>
{{- end}}
{{- if .TaskItemsJSON}}

<TASK_ITEMS>
说明：已完成项内联产出小字段（short_summary / key_facts）与文件指针（step_file / result_file / timeline_file），承接与核实直接消费，无需重新发现；tool_calls_digest 顺 timeline_file 指针按需回读，不内联。
{{.TaskItemsJSON}}
</TASK_ITEMS>
{{- end}}
{{- if .TopicsJSON}}

<TOPICS>
说明：既有业务 lane 清单。completed/blocked 项已收束（runtime 自动保留，不必重复提交）；pending 项由本轮 plan 承接：继续释放其 step、或显式提交 status=blocked 收束。新 lane 直接新增条目。
{{.TopicsJSON}}
</TOPICS>
{{- end}}
{{- if .PlannerJournalPath}}
计划全量历史 journal（plan 真相源，按需回读；jsonl 逐行 append，最新状态 = 同 id 最后一条）：`{{.PlannerJournalPath}}`
{{- end}}
{{- if .IntentContextJSON}}

<INTENT_CONTEXT>
说明：意图恢复上下文。latest_input=用户最新输入，action=推荐后续动作（carry 沿用既有意图理解并承接新输入 / replan 基于新输入重建意图理解 / cold_start 重开）。据此产出 goal_understanding（猜测意图）。
{{.IntentContextJSON}}
</INTENT_CONTEXT>
{{- end}}
{{- if .ReplanContextJSON}}

<REPLAN_CONTEXT>
{{.ReplanContextJSON}}
</REPLAN_CONTEXT>
{{- end}}
{{- if .RecoveryContextJSON}}

<RECOVERY_CHILD_AGENTS>
恢复回合：上一个 step 在中断前未被综合，其子 agent 产出对你不可见。以下为中断点子 agent 现场指针与中断 step 的盘上产出目录，据此续跑或综合其产出。
{{.RecoveryContextJSON}}
</RECOVERY_CHILD_AGENTS>
{{- end}}
{{- if not (or .TaskItemsJSON .ReplanContextJSON .IntentContextJSON .RecoveryContextJSON)}}

<PLANNING_HINT>
这是一个全新任务：无已有执行状态可承接，计划须基于对任务相关上下文的实际了解，而非凭空假设。
</PLANNING_HINT>
{{- end}}
