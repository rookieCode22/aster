rules:
  - id: javascript-misc-sensitive-data-in-log
    patterns:
      - pattern-either:
          - pattern: console.$METHOD(..., $SENSITIVE, ...)
          - pattern: console.$METHOD($A + $SENSITIVE)
          - pattern: console.$METHOD(`...${$SENSITIVE}...`)
          - pattern: $LOG.$METHOD(..., $SENSITIVE, ...)
          - pattern: $LOG.$METHOD($A + $SENSITIVE)
          - pattern: $LOG.$METHOD(`...${$SENSITIVE}...`)
      - metavariable-regex:
          metavariable: $METHOD
          regex: (?i)(log|info|debug|warn|error|trace|fatal|verbose)
      - metavariable-regex:
          metavariable: $SENSITIVE
          regex: (?i)^(?=[a-zA-Z_$]).*(?:password|passwd|secret|token|credential|apiKey|api_key|privateKey|private_key|passphrase|accessKey|access_key|secretKey|secret_key)
    message: >
      Sensitive data may be written to log output. The variable '$SENSITIVE'
      appears to contain sensitive information and is passed to a logging
      call. This can expose credentials in log files, monitoring systems, or
      error tracking tools. Remove sensitive data from log statements or
      mask/redact it before logging.
    metadata:
      cwe: CWE-532
      owasp: "A09:2021"
      confidence: MEDIUM
      needs_dataflow_confirmation: false
    severity: WARNING
    languages: [javascript, typescript]
